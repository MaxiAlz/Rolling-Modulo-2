//ejersicio 1

const meses=["enero", "febrero", "marzo", "abril", "mayo","junio"]

//el parametro de entrada es con el que va a trabajr la funcion, puede ser un arreglo un string, etc
const imprimirLista = (parametroDeEntrada) => {
    for(let i=0; i < parametroDeEntrada.length ; i++){
        console.log(`${parametroDeEntrada[i]} `);
    }
}

imprimirLista(meses);

//ejersicio 3

